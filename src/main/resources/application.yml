ribbon:
  eureka:
   enabled: false
beacon:
  ribbon:
    isSecure: true
    listOfServers: https://beacon-network.org:443
server:
  port : 8095
feign:
  client:
    config:
      default:
        connectTimeout: 30000
        readTimeout: 30000